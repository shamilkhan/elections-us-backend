import { CityElectionResult } from './city-election-result.entity';

describe('CityElectionResult', () => {
  it('should be defined', () => {
    expect(new CityElectionResult()).toBeDefined();
  });
});
