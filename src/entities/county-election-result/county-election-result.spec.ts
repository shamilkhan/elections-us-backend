import { CountyElectionResult } from './county-election-result.entity';

describe('CountyElectionResult', () => {
  it('should be defined', () => {
    expect(new CountyElectionResult()).toBeDefined();
  });
});
